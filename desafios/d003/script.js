function verificar() {
    let num = Number(prompt('Digite um número inteiro qualquer'))
    let ant = num - 1
    let suc = num + 1
    alert(`Antes de ${num} temos ${ant}.
Depois de ${num} temos ${suc}.`)
}